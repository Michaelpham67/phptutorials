<?php  

  $radius = 25; // int
  $pi = 3.14; // float or double

  // basic operators - *(multiply), /(divide), +, -, ** (power)
  // echo $pi * $radius**2;

  // order of operations PEMDAS (), **, *, /, +, - 
  // echo $radius * (4 + 9) / $pi;

  // increment & decrement operators
  // $radius++ is the same as $radius + 1 likewise for $pi-- the same as $pi - 1 

  // echo $radius++;
  // echo $radius;
 
  // shorthand operators
  // $age += 10 is the same as $age = $age + 10 or $age-= 10 same as ...

  // number functions

  // echo floor($pi); //rounds down the integer
  // echo ceil($pi); //rounds up the integer

  // echo pi(); returns pi 

?>

<!DOCTYPE html> 
<html>
<head>
	<title>PHP Tutorials</title>
</head>
<body>


</body>
</html> 